import React from 'react';
import { NavBarButton } from './Header.styles';

class Header extends React.Component {
    render() {
        return <NavBarButton>WOMEN</NavBarButton>;
    }
}

export default Header;
